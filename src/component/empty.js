import { always } from 'ramda';
/**
 * A simple wrapping component for rendering a null element.
 * @name EmptyComponent
 * @returns {null}
 */
export const EmptyComponent = always(null);

export default EmptyComponent;

